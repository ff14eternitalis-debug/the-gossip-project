<div class="container mt-5">
  <h1>Modifier le potin</h1>
  <%= render "form" %>
  <div class="mt-3">
    <%= link_to "Retour au potin", gossip_path(@gossip), class: "btn btn-outline-secondary" %>
  </div>
</div>
