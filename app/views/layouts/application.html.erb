<!DOCTYPE html>
<html data-bs-theme="dark">
  <head>
    <title><%= content_for(:title) || "The Gossip Project" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="The Gossip Project">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <%= link_to "The Gossip Project", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <%= form_tag search_path, method: :get, class: "d-flex me-3" do %>
            <%= text_field_tag :q, params[:q], class: "form-control form-control-sm", placeholder: "Rechercher..." %>
          <% end %>
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <%= link_to "Accueil", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Team", team_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Contact", contact_path, class: "nav-link" %>
            </li>
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to "Mon fil", feed_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <% unread_count = current_user.notifications.unread.count %>
                <%= link_to notifications_path, class: "nav-link" do %>
                  Notifications<% if unread_count > 0 %> <span class="badge bg-danger"><%= unread_count %></span><% end %>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to "Messagerie", conversations_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Mon compte", user_path(current_user), class: "nav-link" %>
              </li>
              <% if current_user.admin? %>
                <li class="nav-item">
                  <%= link_to "Admin", admin_dashboard_path, class: "nav-link" %>
                </li>
              <% end %>
              <li class="nav-item">
                <%= button_to "Deconnexion", destroy_user_session_path, method: :delete, class: "nav-link btn btn-link border-0" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Inscription", new_user_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <%= render "shared/flash_messages" %>

    <%= yield %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  </body>
</html>
