<%- if controller_name != 'sessions' %>
  <%= link_to "Connexion", new_session_path(resource_name), class: "btn btn-outline-secondary btn-sm me-2" %>
<% end %>
<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <%= link_to "Inscription", new_registration_path(resource_name), class: "btn btn-outline-secondary btn-sm me-2" %>
<% end %>
<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <%= link_to "Mot de passe oublié ?", new_password_path(resource_name), class: "btn btn-link btn-sm p-0" %>
<% end %>
